import React from 'react'

export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>Web Data Indexer — Dashboard (scaffold)</h1>
      <p>This is a minimal React + D3 scaffold. Build graphs and search UI here.</p>
    </div>
  )
}
